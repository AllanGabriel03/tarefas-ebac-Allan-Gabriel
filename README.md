Exercício 1: Sim, consigo calcular com uma solução recursiva, porém exige mais processamento para realizar diversos cálculos.

Exercício 2: enquanto a solução recalcula todos os números, a solução com programação dinâmica armazena os números já calculados e os reutiliza, evitando assim a recomputação.

Exercício 3: a solução recursiva é O(n), TopDown é O(n), BottomUp é O(n). as 3 soluções tem complexidade de tempo O(n), mas em termos de eficiência a solução BottomUp é a mais eficiente, pois não usa pilha recursiva e é mais estável para valores grandes de n.
